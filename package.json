{
  "name": "votes_calculator_for_stage_for_cinderella",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "format": "prettier --parser typescript --single-quote --list-different \"src/**/*.{ts,tsx}\"",
    "format:write": "prettier --parser typescript --single-quote --write \"src/**/*.{ts,tsx}\""
  },
  "dependencies": {
    "preact": "^10.9.0"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.3.0",
    "@testing-library/preact": "^3.2.2",
    "@testing-library/preact-hooks": "^1.1.0",
    "@types/jest": "^28.1.6",
    "jest": "^28.1.3",
    "jest-environment-jsdom": "^28.1.3",
    "prettier": "^2.7.1",
    "ts-jest": "^28.0.7",
    "typescript": "^4.6.4",
    "vite": "^3.0.0"
  }
}
